			<div style="float:left">
<%= render 'shared/side_menu' %>
</div>

<br>
<div class="row">
	<div class="span8 offset3" style="min-height:500px;">
		<div class = "hero-unit" style="border:1px solid #0088cc;border-radius:5px;">
		<h3 align = "center">Teaching Staff </h3>
<%= simple_form_for(@teachingstaff) do |f| %>
<%= render 'shared/error_messages', object: f.object %>

					<%= f.fields_for :teaching_staff_user do |user_form| %>
			    <table> 
			        <tbody><tr> 
			        	<td>Name:</td>
			            <td> 
			            <%= user_form.text_field :name, :autofocus => true %>
			              </td> 
			        </tr> 
			        <tr> <td>Email:</td>
			            <td> 
			            <%= user_form.text_field :email, :required => true %>
			              </td> 
			        </tr> 
			        <tr> 
			        	<td>Password:</td>
			            <td> 
			            <%= user_form.password_field :password, :required => true %>
			              </td> 
			        </tr> 
			        <tr> 
			        	<td>PasswordConfirmation:</td>
			            <td> 
			            <%= user_form.password_field :password_confirmation, :required => true %>
			              </td> 
			        </tr> 

			        <%end %>
							<% courses_array = Role.all.map { |role| [role.name, role.id] } %>
							<%= f.select(:user_role,options_for_select(courses_array))%>
							<% [ 'admin', 'desert', 'green', 'corporate', 'funky' ].each do |theme| %>
      <br><%= radio_button_tag 'theme', theme %>
      <%= theme.humanize %>
      <% end %>
			        <tr><td>Description</td><td><%= f.text_field :description %></td></tr>
			 		   <tr><td>Qualification</td><td> <%= f.text_field :qualification %></td></tr>
			        


			  	<tr><td></td><td><%= f.submit "save", class: "btn  btn-primary" %></td></tr>
			   
			  
			<% end %></table>
		</div>
	</div>
</div>